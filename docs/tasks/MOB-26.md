# MOB-26 — Telemetry flush

Thêm cơ chế gửi batch telemetry tới endpoint analytics và chỉ xóa queue sau khi server xác nhận thành công.

## Trạng thái

Partial — helper đã có nhưng chưa được gọi từ app lifecycle hoặc background flush.
