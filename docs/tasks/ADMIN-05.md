# ADMIN-05 — Dashboard số liệu cơ bản

Endpoint ADMIN `GET /admin/dashboard` trả tổng user/active user, plan, active place, published event, review và rating trung bình. Các aggregate query chạy song song và không trả dữ liệu cá nhân.
