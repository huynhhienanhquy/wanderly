# AI-05 — Retry và fallback

## Trạng thái

Done — 2026-08-18.

## Kết quả

- OpenAI call có timeout 8 giây và tối đa 2 lần thử cho HTTP lỗi, timeout, JSON/output sai.
- Sau khi provider thất bại, service dùng parser cục bộ để lấy số người và ngân sách phổ biến.
- Fallback luôn trả warning rõ ràng, field còn thiếu và giữ input trong notes để người dùng xác nhận.
- Endpoint không bị treo hoặc thất bại hoàn toàn khi thiếu key/provider gián đoạn.

## Kiểm thử

- Test fallback deterministic khi provider timeout.
- API suite và typecheck workspace đạt.
