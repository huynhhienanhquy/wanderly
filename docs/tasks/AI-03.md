# AI-03 — Tích hợp structured output

## Trạng thái

Done — 2026-08-18.

## Kết quả

- Thêm `POST /ai/constraints` và OpenAI Responses API adapter.
- Request dùng `text.format` JSON Schema strict, `store: false`, model/key lấy từ environment.
- Response được lấy từ `output_text`, parse JSON và validate lại bằng Zod contract trước khi trả client.
- Thiếu key, HTTP lỗi hoặc output rỗng trả lỗi service unavailable có kiểm soát.

## Kiểm thử

- Provider test mock toàn bộ network, kiểm tra structured-output request và parsed response.
- Thiết kế request đối chiếu official OpenAI Structured Outputs documentation.
