# MOB-19 — Reviews native

Thêm màn hình đọc review theo `placeId`, parse dữ liệu bằng `reviewSchema`, hiển thị rating/nội dung/ngày và trạng thái lỗi. Kiểm tra bằng `pnpm --filter @wanderly/mobile typecheck`.
