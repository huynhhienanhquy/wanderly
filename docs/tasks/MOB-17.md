# MOB-17 — Events native

Thêm màn hình sự kiện native gọi `GET /events`, parse bằng contract dùng chung, hiển thị lịch diễn ra và mở booking URL. Kiểm tra bằng `pnpm --filter @wanderly/mobile typecheck`.
