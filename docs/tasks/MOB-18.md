# MOB-18 — Profile native

Thêm màn hình hồ sơ native với đọc/cập nhật `GET/PATCH /profile`, xác thực bằng mobile API client và trạng thái lỗi/thành công. Kiểm tra bằng `pnpm --filter @wanderly/mobile typecheck`.
