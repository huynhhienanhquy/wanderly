import { useLocalSearchParams } from 'expo-router';
import { useState } from 'react';
import { Pressable, StyleSheet, Text, TextInput, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { mobileConfig } from '../src/app-config';
export default function ReviewReportScreen() { const { reviewId } = useLocalSearchParams<{ reviewId: string }>(); const [reason, setReason] = useState(''); const [message, setMessage] = useState(''); async function report() { const response = await fetch(`${mobileConfig.apiUrl}/reviews/${reviewId}/reports`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ reason }) }); setMessage(response.ok ? 'Đã gửi báo cáo.' : 'Không thể gửi báo cáo.'); } return <SafeAreaView style={styles.safe}><View style={styles.container}><Text style={styles.title}>Báo cáo đánh giá</Text><TextInput multiline value={reason} onChangeText={setReason} placeholder="Lý do báo cáo" style={styles.input}/><Pressable disabled={reason.trim().length < 3} onPress={() => void report()} style={styles.button}><Text style={styles.buttonText}>Gửi báo cáo</Text></Pressable>{!!message && <Text style={styles.message}>{message}</Text>}</View></SafeAreaView>; }
const styles = StyleSheet.create({ safe: { backgroundColor: '#f4f7f2', flex: 1 }, container: { padding: 24 }, title: { color: '#17231f', fontSize: 32, fontWeight: '700' }, input: { backgroundColor: '#fff', borderColor: '#d8e1dc', borderRadius: 12, borderWidth: 1, minHeight: 140, marginTop: 24, padding: 14, textAlignVertical: 'top' }, button: { backgroundColor: '#9d2922', borderRadius: 12, marginTop: 18, padding: 14 }, buttonText: { color: '#fff', fontWeight: '700', textAlign: 'center' }, message: { color: '#52615b', marginTop: 14 } });
