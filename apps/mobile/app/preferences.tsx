import { useState } from 'react';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
const OPTIONS = ['cafe', 'food', 'photography', 'nature', 'shopping'];
export default function PreferencesScreen() { const [selected, setSelected] = useState<string[]>([]); return <SafeAreaView style={styles.safe}><View style={styles.container}><Text style={styles.title}>Sở thích của bạn</Text><Text style={styles.body}>Chọn chủ đề để AI gợi ý phù hợp hơn.</Text><View style={styles.grid}>{OPTIONS.map((option) => <Pressable key={option} onPress={() => setSelected((current) => current.includes(option) ? current.filter((value) => value !== option) : [...current, option])} style={[styles.chip, selected.includes(option) && styles.active]}><Text style={styles.chipText}>{option}</Text></Pressable>)}</View><Text style={styles.saved}>Đã chọn {selected.length} sở thích</Text></View></SafeAreaView>; }
const styles = StyleSheet.create({ safe: { backgroundColor: '#f4f7f2', flex: 1 }, container: { padding: 24 }, title: { color: '#17231f', fontSize: 34, fontWeight: '700' }, body: { color: '#52615b', marginTop: 12 }, grid: { flexDirection: 'row', flexWrap: 'wrap', gap: 10, marginTop: 24 }, chip: { backgroundColor: '#fff', borderColor: '#d8e1dc', borderRadius: 20, borderWidth: 1, paddingHorizontal: 16, paddingVertical: 11 }, active: { backgroundColor: '#dcece4', borderColor: '#277253' }, chipText: { color: '#18563d', fontWeight: '600' }, saved: { color: '#52615b', marginTop: 24 } });
