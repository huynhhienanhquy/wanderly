import { useEffect, useState } from 'react'; import { Pressable, StyleSheet, Text, View } from 'react-native'; import { SafeAreaView } from 'react-native-safe-area-context'; import { getMobileTheme, saveMobileTheme, type MobileTheme } from '../src/settings-storage';
export default function SettingsScreen() { const [theme, setTheme] = useState<MobileTheme>('light'); useEffect(() => { getMobileTheme().then(setTheme); }, []); async function toggle() { const next = theme === 'light' ? 'dark' : 'light'; setTheme(next); await saveMobileTheme(next); } return <SafeAreaView style={styles.safe}><View style={styles.container}><Text style={styles.title}>Cài đặt</Text><Pressable accessibilityRole="switch" accessibilityState={{ checked: theme === 'dark' }} onPress={() => void toggle()} style={styles.row}><Text style={styles.label}>Giao diện tối</Text><Text style={styles.value}>{theme === 'dark' ? 'Bật' : 'Tắt'}</Text></Pressable></View></SafeAreaView>; }
const styles = StyleSheet.create({ safe: { backgroundColor: '#f4f7f2', flex: 1 }, container: { padding: 24 }, title: { color: '#17231f', fontSize: 34, fontWeight: '700' }, row: { alignItems: 'center', backgroundColor: '#fff', borderRadius: 14, flexDirection: 'row', justifyContent: 'space-between', marginTop: 24, padding: 18 }, label: { color: '#17231f', fontSize: 16, fontWeight: '600' }, value: { color: '#277253', fontWeight: '700' } });
