import { eventListSchema } from '@wanderly/contracts';
import { useEffect, useState } from 'react';
import { ActivityIndicator, Linking, Pressable, StyleSheet, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { mobileConfig } from '../src/app-config';
import { emptyStateMessage } from '../src/mobile-hardening';
export default function EventsScreen() { const [events, setEvents] = useState<ReturnType<typeof eventListSchema.parse>>([]); const [error, setError] = useState(''); useEffect(() => { fetch(`${mobileConfig.apiUrl}/events`).then(async (response) => { if (!response.ok) throw new Error('Không thể tải sự kiện.'); return eventListSchema.parse(await response.json()); }).then(setEvents).catch((cause: unknown) => setError(cause instanceof Error ? cause.message : 'Không thể tải sự kiện.')); }, []); return <SafeAreaView style={styles.safe}><View style={styles.container}><Text style={styles.eyebrow}>SỰ KIỆN</Text><Text style={styles.title}>Đang diễn ra gần bạn</Text>{!events.length && !error && <ActivityIndicator color="#277253" style={styles.loader} />}{!!error && <Text style={styles.error}>{error}</Text>}{!error && !events.length && <Text style={styles.body}>{emptyStateMessage(events.length, 'sự kiện')}</Text>}{events.map((event) => <View key={event.id} style={styles.card}><Text style={styles.name}>{event.title}</Text><Text style={styles.time}>{new Date(event.startTime).toLocaleString('vi-VN')}</Text><Text style={styles.description}>{event.description ?? 'Khám phá sự kiện mới cùng Wanderly.'}</Text>{event.bookingUrl && <Pressable onPress={() => void Linking.openURL(event.bookingUrl!)}><Text style={styles.link}>Đặt chỗ</Text></Pressable>}</View>)}</View></SafeAreaView>; }
const styles = StyleSheet.create({ safe: { backgroundColor: '#f4f7f2', flex: 1 }, container: { padding: 24 }, eyebrow: { color: '#277253', fontWeight: '700', letterSpacing: 2 }, title: { color: '#17231f', fontSize: 34, fontWeight: '700', marginTop: 8 }, loader: { marginTop: 32 }, error: { color: '#9d2922', marginTop: 20 }, body: { color: '#52615b', marginTop: 16 }, card: { backgroundColor: '#fff', borderRadius: 16, marginTop: 16, padding: 16 }, name: { color: '#17231f', fontSize: 19, fontWeight: '700' }, time: { color: '#277253', fontSize: 13, marginTop: 8 }, description: { color: '#52615b', lineHeight: 21, marginTop: 8 }, link: { color: '#277253', fontWeight: '700', marginTop: 12 } });
