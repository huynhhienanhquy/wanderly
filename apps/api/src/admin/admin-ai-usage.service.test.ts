import { expect, it, vi } from 'vitest'; import type { PrismaService } from '../database/prisma.service'; import { AdminAiUsageService } from './admin-ai-usage.service';
it('aggregates AI latency, failures, tokens and estimated cost', async () => { const findMany = vi.fn().mockResolvedValue([{ provider: 'openai', model: 'gpt-4.1-mini', status: 'SUCCESS', latencyMs: 100, inputTokens: 1000, outputTokens: 500 }, { provider: 'openai', model: 'gpt-4.1-mini', status: 'FAILED', latencyMs: 300, inputTokens: null, outputTokens: null }]); const result = await new AdminAiUsageService({ aiInteraction: { findMany } } as unknown as PrismaService).summary(); expect(result).toEqual([{ provider: 'openai', model: 'gpt-4.1-mini', requests: 2, failures: 1, inputTokens: 1000, outputTokens: 500, estimatedCostUsd: 0.0012, averageLatencyMs: 200 }]); });
